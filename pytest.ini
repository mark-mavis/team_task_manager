[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    unit: Pure unit tests (no HTTP, minimal DB)
    integration: Integration tests hitting FastAPI routes
    e2e: End-to-end browser tests using Playwright
